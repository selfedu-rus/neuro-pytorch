import torch

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

t1 = torch.arange(1, 6, dtype=torch.int32)
t2 = torch.ones(3, 5, dtype=torch.float32)

t_res = torch.matmul(t2, t1.float()).cpu()
